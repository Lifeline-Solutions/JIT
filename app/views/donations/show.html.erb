<p style="color: green"><%= notice %></p>

<%= render @donation %>

<div>
  <%= link_to "Edit this donation", edit_donation_path(@donation) %> |
  <%= link_to "Back to donations", donations_path %>

  <%= button_to "Destroy this donation", @donation, method: :delete %>
</div>



<h2>
 Current STate of this donation
</h2>

<%=  @donation.states.first.name %>

<% if @donation.state_counter == 0 %>
<%= link_to "New State", new_donation_state_path(@donation) %>
<% end %>